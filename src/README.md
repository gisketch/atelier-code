# Source

Atelier uses the v1 reference stack from [../SPEC.md](../SPEC.md):

- `ui/`: React, TypeScript, and Vite board UI.
- `daemon/`: Bun TypeScript orchestrator daemon.
- `shared/`: TypeScript contracts shared by UI and daemon.

The Tauri Rust shell lives in [../src-tauri](../src-tauri).
